import { ComingSoon } from "@/components/ComingSoon";

export default function MindsetPage() {
  return (
    <ComingSoon
      title="Mindset & Fitness"
      blurb="A daily energy, workout, and reflection check-in with a trend view. An optional add-on, not scoped yet, and never a gate on any other module."
    />
  );
}
