import { ComingSoon } from "@/components/ComingSoon";

export default function CrmPage() {
  return (
    <ComingSoon
      title="CRM"
      blurb="A future module for pipeline and account relationships, built on top of the Keap integration seam already reserved in lib/integrations."
    />
  );
}
